[reminder]
data_location=sample
;lang=CHT

[image]
target=sample/demo_specify_image.list
slideshow_frequency=30 ;the frequency to have slideshow on image (sec.)

[search]
;need specify 'api_key' and 'cx' for more image can be searched (and then be added to database)
api_key=
cx=
search_latency=3 ;the frequency to have image search, the lower the more frequent to do search (1-n)
